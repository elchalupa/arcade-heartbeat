# arcade-heartbeat dependencies

# Twitch IRC client
# Pinned to v2.x - v3.0 introduced breaking API changes requiring
# client_id, client_secret, and bot_id which are unnecessary for read-only chat
twitchio>=2.8.0,<3.0.0

# Windows toast notifications
winotify>=1.1.0

# Configuration
pyyaml>=6.0.1

# Environment variables
python-dotenv>=1.0.0

# CLI colors and formatting
colorama>=0.4.6

# Async support (included with twitchio, but explicit)
aiohttp>=3.9.0
